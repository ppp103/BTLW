@model IEnumerable<QuanLyBanSach.Models.Admins>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.MatKhau)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TenDangNhap)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.HoTenAd)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.MatKhau)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TenDangNhap)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.HoTenAd)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.MaAd">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.MaAd">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.MaAd">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
